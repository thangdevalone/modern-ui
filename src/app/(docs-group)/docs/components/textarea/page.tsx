import { Metadata } from "next";

export const metadata: Metadata = {
  title: "TextArea - Modern UI",
  description: "A form textarea component that provides a clean and accessible way to collect multi-line user input.",
};

export default function TextAreaPage() {
  return null;
} 